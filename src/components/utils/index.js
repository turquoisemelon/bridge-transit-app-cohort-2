// export const selectSavedAddress = (state) => {
//   const savedAddressLat = state.address.savedAddress.geometry.location.lat;
//   const savedAddressLng = state.address.savedAddress.geometry.location.lng;
//   const savedAddress = {
//     lat: savedAddressLat,
//     lng: savedAddressLng
//   }
//   const geolocation = state.address.geolocation;
//   const currentUserAddress;
//
//   if (savedAddress) {
//     currentUserAddress = savedAddress;
//   } else if (geolocation) {
//     currentUserAddress = geolocation
//   } else {
//
//   }
//   return currentUserAddress;
// }


// savedAddress:selectSavedAddress(state)
